import { VisualComponent } from '../core/index';
import { IDataSource } from '../data/index';
import { Point } from '../model/index';
export declare class ChartBoard extends VisualComponent {
    private readonly container;
    private readonly offsetLeft;
    private readonly offsetTop;
    private readonly w;
    private readonly h;
    private table;
    private readonly chartAreas;
    private readonly chartStacks;
    private readonly yAxisAreas;
    private readonly yAxes;
    private timeAxis;
    private timeArea;
    private readonly indicators;
    private eventHandlers;
    private readonly mouseHandlers;
    constructor(container: HTMLElement, offsetLeft: number, offsetTop: number, w: number, h: number);
    private insertRow(table, index?, el1?, el2?, el3?);
    private makeArea(w, h);
    addChart<T>(chartType: string, dataSource: IDataSource<T>): void;
    addIndicator(indicatorDataSource: IDataSource<Point>): void;
    render(): void;
    private renderLayers(renderBase, renderFront);
    private onDataChanged(arg);
    onMouseWheel(event: any): void;
    private isMouseEntered;
    private isMouseDown;
    private mouseX;
    private mouseY;
    onMouseMove(event: any): void;
    onMouseEnter(event: any): void;
    onMouseLeave(event: any): void;
    onMouseUp(event: any): void;
    onMouseDown(event: any): void;
}
